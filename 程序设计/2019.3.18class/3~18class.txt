第三章 	数据链路层
三个基本问题：
	封装成帧：定界
	透明传输：发送方加转义，接收方去掉转义字节
	差错检测
		CRC  FCS
		模二运算
		“被除数”，M比特的数据+n个0
		“除数”，事先约定n+1位（生成多项式）
		“余数”，n位（高位补0）
	发送方：M比特的数据+n位的冗余码
	接收方：用收到的所有数据，通过事先约定的除数进行约定
		余数为0，则无比特差错
		余数非0，则有比特差错
	
	      总结：一旦发现差错，将数据帧丢弃
    		CRC能实现无比特差错接收，但是不能实现可靠传输


点对点的信道

广播信道	












